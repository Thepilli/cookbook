import 'package:flutter/material.dart';

class FlatJson extends StatelessWidget {
  const FlatJson({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Flat JSON')),
      body: ListView.builder(
        itemBuilder: (context, index) {
          return null;
        },
      ),
    );
  }
}
